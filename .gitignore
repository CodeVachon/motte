node_modules
dist
bin
.vitest
coverage

# Generated by `bun run build:web` — embeds the built SPA into the binary.
packages/cli/src/generated

# Astro / Vite
.astro
apps/*/dist
apps/*/.vite

.DS_Store
*.log
.env
.env.local

# NOTE: `.motte/` is deliberately NOT ignored. The backlog is the product of this
# tool and is meant to be committed and shared across workspaces.

# Playwright MCP writes snapshots and screenshots here while verifying the web UI.
.playwright-mcp/
